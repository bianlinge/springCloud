<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!--当前项目在整个分布式架构里面的唯一名称，计算依赖关系的标签-->
    <dubbo:application name="dove-providers" owner="dove"/>

    <!--监控中心配置， 用于配置连接监控中心相关信息，可以不配置，不是必须的参数。-->
    <dubbo:monitor protocol="registry"/>

    <!--dubbo这个服务所要暴露的服务地址所对应的注册中心 N/A 无注册中心-->
    <!--N/A 表示由 dubbo 自动分配地址。或者说是一种直连的方式，不通过注册中心。-->
    <!--点对点式-->
    <!--<dubbo:registry address="N/A"/>-->

    <!--引入zookeeper注册中心-->
    <dubbo:registry address="zookeeper://192.168.0.9:2181"/>

    <!--当前服务发布所依赖的协议；支持RPC协议：RMI，webserovice、Thrift、Hessain、http,默认dubbo协议-->
    <dubbo:protocol name="dubbo" port="20880"/>

    <!--服务发布的配置，需要暴露的服务接口-->
    <dubbo:service interface="com.dove.dubbo.ProviderService" ref="providerService"/>

    <!--Bean 具体服务实现bean定义-->
    <bean id="providerService" class="com.dove.dubbo.ProviderServiceImpl"/>

</beans>
